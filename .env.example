# Copy this file to .env and fill in the values. .env is git-ignored, so nothing
# here reaches the repo. run_weekly.sh loads .env because launchd/cron does not
# read your shell profile (~/.zshrc).
ANTHROPIC_API_KEY=sk-ant-...
LLM_MODEL=          # model id for the LLM classification/extraction step
