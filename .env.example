# Copy this file to .env and fill in your key. .env is git-ignored, so your key
# stays out of the repo. run_weekly.sh loads .env because launchd/cron does not
# read your shell profile (~/.zshrc).
ANTHROPIC_API_KEY=sk-ant-...
